# This file lists the dependencies for the project. To install them, run:
# pip install -r requirements.txt   / If you are using a virtual environment, make sure to activate it before running the above command.
pandas>=2.0.0
openpyxl>=3.1.0
streamlit>=1.28.0
plotly>=5.18.0
numpy>=1.24.0